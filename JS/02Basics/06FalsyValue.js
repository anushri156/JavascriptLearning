// Falsy values are:
// null
// undefined
// 0
// ""
// NaN     (Not a Number)

// == compares only value
var user="2";
if(user==2)
console.log(`Everything is Ok`);

// === checks/compare datatype
var user="2";
if(user === 2)
console.log(`Everything is ok`);